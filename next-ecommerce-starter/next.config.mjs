/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['images.pexels.com','static.wixstatic.com'], // Add this domain
        
      },
};

export default nextConfig;
